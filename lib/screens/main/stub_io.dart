// Web stub: never actually called on mobile - just satisfies the import.
import 'dart:typed_data';

class _StubFile {
  final String path;
  _StubFile(this.path);
}

Future<dynamic> saveBytesToTempFile(Uint8List bytes, String filename) async {
  throw UnsupportedError('saveBytesToTempFile not supported on this platform');
}
